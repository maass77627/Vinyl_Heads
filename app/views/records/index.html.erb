<center>

<% @records.each do |record| %><br>
    
    <% end %>


   <%= form_tag(records_path, method: :get) do %>
   <%= text_field_tag(:search, params[:search]) %>
    <%= submit_tag ("Search") %>
    <% end %>

  </center>

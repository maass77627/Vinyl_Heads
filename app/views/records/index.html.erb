<Ul>

<% @records.each do |record| %><br>
    @records.image = current_image
     
   <li> <%= image_tag record.image %> </li> 
   <li> <%= link_to record.title, record_path(record) %> </li>
    <% end %>
    </ul>
    
   <%= form_tag(records_path, method: :get) do %>
   <%= text_field_tag(:search, params[:search]) %>
    <%= submit_tag ("Search") %>
    <% end %>
    
  </center>  